apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "bitwarden.icons" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "bitwarden.icons" . }}
    app.kubernetes.io/component: icons
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.icons.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "bitwarden.icons" . }}
  template:
    metadata:
      labels:
        app: {{ template "bitwarden.icons" . }}
        app.kubernetes.io/component: icons
{{ include "bitwarden.labels" . | indent 8 }}
    spec:
      containers:
      - name: {{ template "bitwarden.icons" . }}
        image: "{{ .Values.component.icons.image.name }}:{{ .Values.general.coreVersion }}"
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config-map
          - secretRef:
              {{- if eq .Values.secrets.secretSource "fromValues" }}
              name: {{ .Release.Name }}-globalsettings-env-secrets
              {{- end }}
              {{- if eq .Values.secrets.secretSource "fromSecret" }}
              name: "{{ .Values.secrets.fromSecret.secretName }}"
              {{- end }}  
          - secretRef:
              name: {{ .Release.Name }}-sql-connection-string                      
        env:
          - name: ASPNETCORE_URLS
            value: http://+:5000
        livenessProbe:
          httpGet:
            path: /alive
            port: 5000
          initialDelaySeconds: 180    
        ports:
        - containerPort: 5000
        resources:
{{ toYaml .Values.component.icons.resources | indent 10 }}
      volumes:
---

apiVersion: v1
kind: Service
metadata:
  name: {{ template "bitwarden.icons" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: icons
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.icons.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  selector:
    app: {{ template "bitwarden.icons" . }}
  ports:
  - port: 5000
    targetPort: 5000

