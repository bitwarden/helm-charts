apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "bitwarden.events" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "bitwarden.events" . }}
    app.kubernetes.io/component: events
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.events.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "bitwarden.events" . }}
  template:
    metadata:
      labels:
        app: {{ template "bitwarden.events" . }}
        app.kubernetes.io/component: events
{{ include "bitwarden.labels" . | indent 8 }}
    spec:
      containers:
      - name: {{ template "bitwarden.events" . }}
        image: "{{ .Values.component.events.image.name }}:{{ .Values.general.coreVersion }}"
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config-map
          - secretRef:
              {{- if eq .Values.secrets.secretSource "fromValues" }}
              name: {{ .Release.Name }}-globalsettings-env-secrets
              {{- end }}
              {{- if eq .Values.secrets.secretSource "fromSecret" }}
              name: "{{ .Values.secrets.fromSecret.secretName }}"
              {{- end }}                             
        env:
          - name: ASPNETCORE_URLS
            value: http://+:5000
        livenessProbe:
          httpGet:
            path: /alive
            port: 5000
          initialDelaySeconds: 180    
        ports:
        - containerPort: 5000
        resources:
{{ toYaml .Values.component.events.resources | indent 10 }}
        volumeMounts:           
      volumes:
---

apiVersion: v1
kind: Service
metadata:
  name: {{ template "bitwarden.events" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: events
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.events.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  selector:
    app: {{ template "bitwarden.events" . }}
  ports:
  - port: 5000
    targetPort: 5000

