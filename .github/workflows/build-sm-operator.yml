name: Build SM Operator

on:
  push:
    branches:
      - BRE-131-split-workflows
    paths-ignore:
      - ".github/workflows/**"
  workflow_dispatch:

jobs:
  build-sm-operator:
    uses: ./.github/workflows/_build.yml
    with:
      chart_name: sm-operator