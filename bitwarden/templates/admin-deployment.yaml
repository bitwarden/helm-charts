{{- if and .Values.component.admin.deployment.enabled -}}
---
apiVersion: apps/v1
kind: {{ .Values.component.admin.deployment.kind }}
metadata:
  name: {{ template "bitwarden.admin" . }}
{{- with .Values.component.admin.deployment.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: admin
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.admin.deployment.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.component.admin.deployment.kind "Deployment" }}
  replicas: {{ .Values.component.admin.deployment.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: admin
  template:
    metadata:
{{- with .Values.component.admin.deployment.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: admin
{{ include "bitwarden.labels" . | indent 8 }}
{{- with .Values.component.admin.deployment.podLabels }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- with .Values.component.admin.deployment.initContainers }}
      initContainers:
{{- toYaml . | indent 8 }}
{{- end }}
      containers:
        - name: {{ template "bitwarden.admin" . }}
          image: "{{ .Values.component.admin.deployment.image.name }}:{{ .Values.component.admin.deployment.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.component.admin.deployment.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env:
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.component.admin.service.targetPort }}
          ports:
            - containerPort: {{ .Values.component.admin.service.targetPort }}
          volumeMounts:
            - name: dataprotection
              mountPath: /etc/bitwarden/core/aspnet-dataprotection
            - name: licenses
              mountPath: /etc/bitwarden/core/licenses
{{- if .Values.component.admin.deployment.additionalContainers }}
{{- toYaml .Values.component.admin.deployment.additionalContainers | indent 8 }}
{{- end }}
      volumes:
        - name: dataprotection
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.dataprotection" . }}
        - name: licenses
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.licenses" . }}
{{- if .Values.component.admin.deployment.additionalVolumes }}
{{- toYaml .Values.component.admin.deployment.additionalVolumes | indent 8 }}
{{- end }}
{{- end -}}
