{{- if .Values.component.events.service.enabled -}}
---
kind: Service
apiVersion: v1
metadata:
  name: {{ template "bitwarden.events" . }}
{{- with .Values.component.events.service.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: events
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.events.service.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.component.events.service.type }}
  ports:
    - name: http-port
      port: {{ .Values.component.events.service.port }}
      targetPort: {{ .Values.component.events.service.targetPort }}
  selector:
    app.kubernetes.io/component: events
{{- end -}}
