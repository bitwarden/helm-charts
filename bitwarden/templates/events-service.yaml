{{- if .Values.service.events.enabled }}
---
kind: Service
apiVersion: v1
metadata:
  name: {{ template "bitwarden.events" . }}
{{- with .Values.service.events.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: events
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.service.events.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.events.type }}
  ports:
    - name: http-port
      port: {{ .Values.service.events.port }}
      targetPort: {{ .Values.service.events.targetPort }}
  selector:
    app.kubernetes.io/component: events
{{- end }}
