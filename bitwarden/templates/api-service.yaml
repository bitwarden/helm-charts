{{- if .Values.service.api.enabled }}
---
kind: Service
apiVersion: v1
metadata:
  name: {{ template "bitwarden.api" . }}
{{- if .Values.service.api.annotations }}
  annotations:
{{ toYaml .Values.service.api.annotations | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: api
{{ include "bitwarden.labels" . | indent 4 }}
{{- if .Values.service.api.labels }}
{{ toYaml .Values.service.api.labels | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.api.type }}
  ports:
    - name: http-port
      port: {{ .Values.service.api.port }}
      targetPort: {{ .Values.service.api.targetPort }}
  selector:
    app.kubernetes.io/component: api
{{- end }}
