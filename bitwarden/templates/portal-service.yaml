{{- if .Values.service.portal.enabled -}}
---
kind: Service
apiVersion: v1
metadata:
  name: {{ template "bitwarden.portal" . }}
{{- with .Values.service.portal.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: portal
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.service.portal.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.portal.type }}
  ports:
    - name: http-port
      port: {{ .Values.service.portal.port }}
      targetPort: {{ .Values.service.portal.targetPort }}
  selector:
    app.kubernetes.io/component: portal
{{- end -}}
