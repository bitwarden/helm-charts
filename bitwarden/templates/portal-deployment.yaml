{{- if .Values.deployment.portal.enabled }}
---
apiVersion: apps/v1
kind: {{ .Values.deployment.portal.kind }}
metadata:
  name: {{ template "bitwarden.portal" . }}
{{- with .Values.deployment.portal.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: portal
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.deployment.portal.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.deployment.portal.kind "Deployment" }}
  replicas: {{ .Values.deployment.portal.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: portal
  template:
    metadata:
{{- with .Values.deployment.portal.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: portal
{{ include "bitwarden.labels" . | indent 8 }}
{{- with .Values.deployment.portal.podLabels }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- with .Values.deployment.portal.initContainers }}
      initContainers:
{{- toYaml . | indent 8 }}
{{- end }}
      containers:
        - name: {{ template "bitwarden.portal" . }}
          image: "{{ .Values.deployment.portal.image.name }}:{{ .Values.deployment.portal.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.deployment.portal.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env:
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.service.portal.targetPort }}
          ports:
            - containerPort: {{ .Values.service.portal.targetPort }}
          volumeMounts:
            - name: dataprotection
              mountPath: /etc/bitwarden/core/aspnet-dataprotection
            - name: licenses
              mountPath: /etc/bitwarden/core/licenses
{{- if .Values.deployment.portal.additionalContainers }}
{{- toYaml .Values.deployment.portal.additionalContainers | indent 8 }}
{{- end }}
      volumes:
        - name: dataprotection
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.dataprotection" . }}
        - name: licenses
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.licenses" . }}
{{- if .Values.deployment.portal.additionalVolumes }}
{{- toYaml .Values.deployment.portal.additionalVolumes | indent 8 }}
{{- end }}
{{- end }}
