{{- if .Values.component.portal.deployment.enabled -}}
---
apiVersion: apps/v1
kind: {{ .Values.component.portal.deployment.kind }}
metadata:
  name: {{ template "bitwarden.portal" . }}
{{- with .Values.component.portal.deployment.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: portal
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.portal.deployment.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.component.portal.deployment.kind "Deployment" }}
  replicas: {{ .Values.component.portal.deployment.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: portal
  template:
    metadata:
{{- with .Values.component.portal.deployment.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: portal
{{ include "bitwarden.labels" . | indent 8 }}
{{- with .Values.component.portal.deployment.podLabels }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- with .Values.component.portal.deployment.initContainers }}
      initContainers:
{{- toYaml . | indent 8 }}
{{- end }}
      containers:
        - name: {{ template "bitwarden.portal" . }}
          image: "{{ .Values.component.portal.deployment.image.name }}:{{ .Values.component.portal.deployment.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.component.portal.deployment.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env:
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.component.portal.service.targetPort }}
          ports:
            - containerPort: {{ .Values.component.portal.service.targetPort }}
          volumeMounts:
            - name: dataprotection
              mountPath: /etc/bitwarden/core/aspnet-dataprotection
            - name: licenses
              mountPath: /etc/bitwarden/core/licenses
{{- if .Values.component.portal.deployment.additionalContainers }}
{{- toYaml .Values.component.portal.deployment.additionalContainers | indent 8 }}
{{- end }}
      volumes:
        - name: dataprotection
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.dataprotection" . }}
        - name: licenses
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.licenses" . }}
{{- if .Values.component.portal.deployment.additionalVolumes }}
{{- toYaml .Values.component.portal.deployment.additionalVolumes | indent 8 }}
{{- end }}
{{- end -}}
