{{- if .Values.component.attachments.deployment.enabled -}}
---
apiVersion: apps/v1
kind: {{ .Values.component.attachments.deployment.kind }}
metadata:
  name: {{ template "bitwarden.attachments" . }}
{{- with .Values.component.attachments.deployment.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: attachments
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.attachments.deployment.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.component.attachments.deployment.kind "Deployment" }}
  replicas: {{ .Values.component.attachments.deployment.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: attachments
  template:
    metadata:
{{- with .Values.component.attachments.deployment.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: attachments
{{ include "bitwarden.labels" . | indent 8 }}
{{- with .Values.component.attachments.deployment.podLabels }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- with .Values.component.attachments.deployment.initContainers }}
      initContainers:
{{- toYaml . | indent 8 }}
{{- end }}
      containers:
        - name: {{ template "bitwarden.attachments" . }}
          image: "{{ .Values.component.attachments.deployment.image.name }}:{{ .Values.component.attachments.deployment.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.component.attachments.deployment.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env:
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.component.attachments.service.targetPort }}
          ports:
            - containerPort: {{ .Values.component.attachments.service.targetPort }}
          volumeMounts:
            - name: attachments
              mountPath: /etc/bitwarden/core/attachments
{{- if .Values.component.attachments.deployment.additionalContainers }}
{{- toYaml .Values.component.attachments.deployment.additionalContainers | indent 8 }}
{{- end }}
      volumes: 
        - name: attachments
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.attachments" . }}
{{- if .Values.component.attachments.deployment.additionalVolumes }}
{{- toYaml .Values.component.attachments.deployment.additionalVolumes | indent 8 }}
{{- end }}
{{- end -}}
