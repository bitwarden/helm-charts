{{- if .Values.component.proxy.deployment.enabled -}}
---
apiVersion: apps/v1
kind: {{ .Values.component.proxy.deployment.kind }}
metadata:
  name: {{ template "bitwarden.proxy" . }}
{{- with .Values.component.proxy.deployment.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: proxy
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.proxy.deployment.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.component.proxy.deployment.kind "Deployment" }}
  replicas: {{ .Values.component.proxy.deployment.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: proxy
  template:
    metadata:
{{- with .Values.component.proxy.deployment.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: proxy
{{ include "bitwarden.labels" . | indent 8 }}
{{- with .Values.component.proxy.deployment.podLabels }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- with .Values.component.proxy.deployment.initContainers }}
      initContainers:
{{- toYaml . | indent 8 }}
{{- end }}
      containers:
        - name: {{ template "bitwarden.proxy" . }}
          image: "{{ .Values.component.proxy.deployment.image.name }}:{{ .Values.component.proxy.deployment.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.component.proxy.deployment.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          ports:
            - containerPort: {{ .Values.component.proxy.service.targetPort }}
          volumeMounts:
            - name: proxy
              mountPath: /etc/nginx/conf.d
{{- if .Values.component.proxy.deployment.additionalContainers }}
{{- toYaml .Values.component.proxy.deployment.additionalContainers | indent 8 }}
{{- end }}
      volumes:
        - name: proxy
          configMap:
            name: {{ template "bitwarden.proxy" . }}
{{- if .Values.component.proxy.deployment.additionalVolumes }}
{{- toYaml .Values.component.proxy.deployment.additionalVolumes | indent 8 }}
{{- end }}
{{- end -}}
