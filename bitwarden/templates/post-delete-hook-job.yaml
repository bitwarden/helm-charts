---
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-cleanup"
  labels:
    app.kubernetes.io/component: post-delete-hook
{{ include "bitwarden.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": post-delete
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: "{{ .Release.Name }}-cleanup"
      labels:
        app.kubernetes.io/component: post-delete-hook
{{ include "bitwarden.labels" . | indent 8 }}
    spec:
      containers:
      - name: delete-resources
        command: [
          "/bin/sh", "-c"
        ]
        args: ['
          kubectl delete configmap {{ .Values.proxyVolumeConfigMapName }} -n {{ .Release.Namespace }};
          kubectl delete configmap {{ .Values.configMapName }} -n {{ .Release.Namespace }};
          kubectl delete secret {{ .Values.identityVolumeSecretName }} -n {{ .Release.Namespace }};
          kubectl delete secret {{ .Values.webVolumeSecretName }} -n {{ .Release.Namespace }};
          echo "Done"
        ']
        image: "bitnami/kubectl:1.19"
      restartPolicy: Never
