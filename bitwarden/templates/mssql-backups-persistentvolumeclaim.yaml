{{- if and (eq .Values.database.type "container") (not .Values.volume.mssql.backups.existingClaim) -}}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "bitwarden.mssqlBackups" . }}
{{- with .Values.volume.mssql.backups.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: mssql-backups
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.volume.mssql.backups.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  accessModes:
{{ toYaml .Values.volume.mssql.backups.accessModes | indent 4 }}
  storageClassName: {{ .Values.volume.mssql.backups.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.volume.mssql.backups.size }}
{{- end -}}
