{{- if and (eq .Values.database.type "container") (not .Values.component.mssql.volume.data.existingClaim) -}}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "bitwarden.mssqlData" . }}
{{- with .Values.component.mssql.volume.data.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: mssql-data
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.mssql.volume.data.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  accessModes:
    - {{ .Values.component.mssql.volume.data.accessMode | quote }}
{{- if .Values.component.mssql.volume.data.storageClass }}
  storageClassName: {{ .Values.component.mssql.volume.data.storageClass | quote }}
{{- end }}
  resources:
    requests:
      storage: {{ .Values.component.mssql.volume.data.size }}
{{- end -}}
