{{- if .Values.deployment.sso.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "bitwarden.sso" . }}
{{- if .Values.deployment.sso.annotations }}
  annotations:
{{ toYaml .Values.deployment.sso.annotations | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: sso
{{ include "bitwarden.labels" . | indent 4 }}
{{- if .Values.deployment.sso.labels }}
{{ toYaml .Values.deployment.sso.labels | indent 4 }}
{{- end }}
spec:
  replicas: {{ .Values.deployment.sso.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: sso
  template:
    metadata:
{{- if .Values.deployment.sso.podAnnotations }}
      annotations:
{{ toYaml .Values.deployment.sso.podAnnotations | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: sso
{{ include "bitwarden.labels" . | indent 8 }}
{{- if .Values.deployment.sso.podLabels }}
{{ toYaml .Values.deployment.sso.podLabels | indent 8 }}
{{- end }}
    spec:
      containers:
        - name: {{ template "bitwarden.sso" . }}
          image: "{{ .Values.deployment.sso.image.name }}:{{ .Values.deployment.sso.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.deployment.sso.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env: 
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.service.sso.targetPort }}
          ports:
            - containerPort: {{ .Values.service.sso.targetPort }}
          volumeMounts:
            - name: identity
              mountPath: /etc/bitwarden/identity
            - name: dataprotection
              mountPath: /etc/bitwarden/core/aspnet-dataprotection
            - name: licenses
              mountPath: /etc/bitwarden/core/licenses
      volumes:
        - name: identity
          secret:
            secretName: {{ template "bitwarden.identity" . }}
        - name: dataprotection
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.dataprotection" . }}
        - name: licenses
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.licenses" . }}
{{- end }}
