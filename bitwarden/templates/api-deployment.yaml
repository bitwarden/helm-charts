{{- if .Values.deployment.api.enabled }}
---
apiVersion: apps/v1
kind: {{ .Values.deployment.api.kind }}
metadata:
  name: {{ template "bitwarden.api" . }}
{{- if .Values.deployment.api.annotations }}
  annotations:
{{ toYaml .Values.deployment.api.annotations | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: api
{{ include "bitwarden.labels" . | indent 4 }}
{{- if .Values.deployment.api.labels }}
{{ toYaml .Values.deployment.api.labels | indent 4 }}
{{- end }}
spec:
{{- if eq .Values.deployment.api.kind "Deployment" }}
  replicas: {{ .Values.deployment.api.replicas }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: api
  template:
    metadata:
{{- if .Values.deployment.api.podAnnotations }}
      annotations:
{{ toYaml .Values.deployment.api.podAnnotations | indent 8 }}
{{- end }}
      labels:
        app.kubernetes.io/component: api
{{ include "bitwarden.labels" . | indent 8 }}
{{- if .Values.deployment.api.podLabels }}
{{ toYaml .Values.deployment.api.podLabels | indent 8 }}
{{- end }}
    spec:
      containers:
        - name: {{ template "bitwarden.api" . }}
          image: "{{ .Values.deployment.api.image.name }}:{{ .Values.deployment.api.image.tag | default .Values.general.coreVersion }}"
          imagePullPolicy: {{ .Values.deployment.api.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.general.configMapName }}
          env:
            - name: ASPNETCORE_URLS
              value: http://+:{{ .Values.service.api.targetPort }}
          ports:
            - containerPort: {{ .Values.service.api.targetPort }}
          volumeMounts:
            - name: attachments
              mountPath: /etc/bitwarden/core/attachments
            - name: dataprotection
              mountPath: /etc/bitwarden/core/aspnet-dataprotection
            - name: licenses
              mountPath: /etc/bitwarden/core/licenses
      volumes:
        - name: attachments
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.attachments" . }}
        - name: dataprotection
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.dataprotection" . }}
        - name: licenses
          persistentVolumeClaim:
            claimName: {{ template "bitwarden.licenses" . }}
{{- end }}
