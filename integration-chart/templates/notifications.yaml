apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "bitwarden.notifications" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "bitwarden.notifications" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "bitwarden.notifications" . }}
  template:
    metadata:
      labels:
        app: {{ template "bitwarden.notifications" . }}
    spec:
      containers:
      - name: {{ template "bitwarden.notifications" . }}
        image: "{{ .Values.component.notifications.image.name }}:{{ .Values.general.coreVersion }}"
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config-map   
          - secretRef:
              {{- if eq .Values.secrets.secretSource "fromValues" }}
              name: {{ .Release.Name }}-globalsettings-env-secrets
              {{- end }}
              {{- if eq .Values.secrets.secretSource "fromSecret" }}
              name: "{{ .Values.secrets.fromSecret.secretName }}"
              {{- end }}                           
        env:
          - name: ASPNETCORE_URLS
            value: http://+:5000
        livenessProbe:
          httpGet:
            path: /alive
            port: 5000
          initialDelaySeconds: 180    
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        volumeMounts:          
      volumes:
---

apiVersion: v1
kind: Service
metadata:
  name: {{ template "bitwarden.notifications" . }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ template "bitwarden.notifications" . }}
  ports:
  - port: 5000
    targetPort: 5000

