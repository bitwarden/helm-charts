{{- $chartVersion := .Chart.Version }}

=====================================================
Bitwarden Self-Host Helm Chart {{ $chartVersion }}
=====================================================

Bitwarden been successfully deployed!

{{- if .Values.general.ingress.enabled }}

NOTE: You have `general.ingress.enabled: true` set.
Ingress is supported but is no longer the recommended default for this chart; Gateway API
is the preferred option for new deployments.
The ingress-nginx controller (this chart's default `className: "nginx"`) was retired by the
upstream Kubernetes project in March 2026 and no longer receives bug fixes or security patches.
Consider migrating to Gateway API (set `general.gateway.enabled: true` and configure
`general.gateway.parentRefs`) or another maintained ingress controller. See the chart README:
https://github.com/bitwarden/helm-charts/tree/main/charts/self-host#gateway-api
{{- end }}
