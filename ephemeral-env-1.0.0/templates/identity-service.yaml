{{- if .Values.component.identity.service.enabled -}}
---
kind: Service
apiVersion: v1
metadata:
  name: {{ template "bitwarden.identity" . }}
{{- with .Values.component.identity.service.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/component: identity
{{ include "bitwarden.labels" . | indent 4 }}
{{- with .Values.component.identity.service.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.component.identity.service.type }}
  ports:
    - name: http-port
      port: {{ .Values.component.identity.service.port }}
      targetPort: {{ .Values.component.identity.service.targetPort }}
  selector:
    app.kubernetes.io/component: identity
{{- end -}}
